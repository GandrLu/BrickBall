#include "brick.h"
#include <QGraphicsScene>

Brick::Brick()
{
    /*setRect(0, 0, 80, 30);
    setFlag(QGraphicsItem::ItemIsFocusable);
    setFocus();
    setPos(scene()->width() * 0.5 - 50, 0);*/
}
